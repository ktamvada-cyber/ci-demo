name: Deploy on Comment

# SECURITY ARCHITECTURE:
# This workflow uses issue_comment trigger which ALWAYS runs from the default branch (main).
# This prevents attackers from modifying the workflow in feature branches to bypass security checks.
# Even if a malicious PR contains a modified .github/workflows/deploy.yml, GitHub will ignore it
# and execute the version from the default branch only.
#
# Reference: https://docs.github.com/en/actions/using-workflows/events-that-trigger-workflows#issue_comment
on:
  issue_comment:
    types: [created]

# Explicit permissions following principle of least privilege
# This workflow can only read code/PRs and write comments - cannot push code or modify settings
permissions:
  contents: read        # Read repository contents for checkout
  pull-requests: write  # Required to read PR metadata in issue_comment context
  issues: write         # Post deployment status comments
  deployments: write    # Create GitHub Deployments via API for correct ref tracking
  checks: read          # Read check runs for coverage gate

# Environment variables (workflow-level)
env:
  # VM deployment configuration constants
  STAGING_PORT: 9001
  PROD_PORT: 9002
  SERVICE_DIR: /var/www/myservice
  HEALTH_CHECK_MAX_RETRIES: 10
  HEALTH_CHECK_RETRY_SLEEP: 2

# Concurrency control: Only ONE deployment at a time across all environments
# Cancel any in-progress deployment when a new one is triggered
# Prevents race conditions and ensures clean container state
concurrency:
  group: deployment
  cancel-in-progress: true

jobs:
  validate-and-deploy:
    # Security: Only run on pull request comments (not regular issues)
    if: github.event.issue.pull_request != null
    runs-on: self-hosted

    # Environment-based deployment protection
    # Production deployments require manual approval from designated reviewers
    # Staging deployments proceed automatically after validation
    environment:
      name: ${{ github.event.comment.body == '[prod]' && 'production' || 'staging' }}
      url: ${{ github.event.comment.body == '[prod]' && 'http://localhost:8002' || 'http://localhost:8001' }}

    # Prevent runaway deployments - fail if job exceeds 15 minutes
    # Note: For production, this timeout includes time waiting for manual approval
    timeout-minutes: 15

    steps:
      # STEP 0: Cleanup orphan processes before deployment
      - name: Ensure no orphan processes
        run: |
          echo "🧹 Cleaning up any orphan processes..."
          pkill -f "uvicorn app.main" || true
          echo "✅ Cleanup complete"

      # STEP 1: Validate deployment trigger and perform all security checks
      - name: Validate deployment trigger and permissions
        id: validate
        uses: actions/github-script@v7
        with:
          script: |
            const comment = context.payload.comment.body.trim();
            const validCommands = ['[staging]', '[prod]'];

            // STRICT COMMAND MATCHING: Only exact "[staging]" or "[prod]" triggers deployment
            // Prevents typos, variations, or injection attempts from triggering deployments
            if (!validCommands.includes(comment)) {
              const errorMsg = `Invalid command: "${comment}". Only exact [staging] or [prod] commands are allowed.`;
              core.setOutput('error_message', errorMsg);
              core.info('Comment does not match deployment triggers: [staging] or [prod]');
              return { shouldDeploy: false };
            }

            // Determine target environment
            const environment = comment === '[staging]' ? 'staging' : 'production';
            core.info(`Deployment requested for: ${environment}`);

            // SECURITY CHECK 1: Verify commenter is authorized for the target environment
            // Different users can deploy to different environments
            const commenter = context.payload.comment.user.login;

            // Define authorized users per environment
            const stagingAuthorizedUsers = [
              'ktamvada-cyber',  // repository owner
              'gromag'           // additional staging deployer
            ];

            const productionAuthorizedUsers = [
             'ktamvada-cyber',  // repository owner
              'gromag'           // ONLY gromag can deploy to production (strict)
            ];

            // Check authorization based on target environment
            const authorizedUsers = environment === 'staging'
              ? stagingAuthorizedUsers
              : productionAuthorizedUsers;

            if (!authorizedUsers.includes(commenter)) {
              const allowedList = authorizedUsers.join(', ');
              const errorMsg = `❌ AUTHORIZATION FAILED: User "${commenter}" is not authorized to deploy to ${environment}. Authorized users: ${allowedList}`;
              core.setOutput('error_message', errorMsg);
              core.setFailed(errorMsg);
              return { shouldDeploy: false };
            }
            core.info(`✅ Authorization check passed: ${commenter} is authorized for ${environment} deployment`);

            // Get PR details
            const { data: pr } = await github.rest.pulls.get({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: context.issue.number
            });

            // SECURITY CHECK 2: Verify PR is OPEN
            // Prevents deployment of closed or merged PRs
            if (pr.state !== 'open') {
              const errorMsg = `❌ VALIDATION FAILED: PR #${context.issue.number} is not open (state: ${pr.state})`;
              core.setOutput('error_message', errorMsg);
              core.setFailed(errorMsg);
              return { shouldDeploy: false };
            }
            core.info(`✅ PR state check passed: PR is open`);

            // SECURITY CHECK 3: Verify mergeable_state is "clean"
            // Prevents deployment if PR has conflicts, is blocked, behind, or in unknown state
            // "clean" means: all required checks passed, no conflicts, ready to merge
            if (pr.mergeable_state !== 'clean') {
              // Check if the issue is a failing test-coverage check
              const prSha = pr.head.sha;
              let coverageErrorMsg = null;

              try {
                const { data: allChecks } = await github.rest.checks.listForRef({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  ref: prSha
                });

                const coverageCheck = allChecks.check_runs.find(check => check.name === 'test-coverage');

                if (coverageCheck && coverageCheck.conclusion === 'failure') {
                  // Extract coverage percentage from check title
                  let coveragePct = 'N/A';
                  if (coverageCheck.output && coverageCheck.output.title) {
                    const match = coverageCheck.output.title.match(/Coverage\s+([\d.]+)%/);
                    if (match) {
                      coveragePct = match[1];
                    }
                  }

                  coverageErrorMsg = `❌ DEPLOYMENT BLOCKED: Coverage Gate Failed\n\n` +
                    `**Coverage:** ${coveragePct}% (threshold: 70%)\n` +
                    `**Status:** Test coverage check failed\n\n` +
                    `---\n\n` +
                    `#### 📊 Coverage Requirements\n\n` +
                    `Deployments require:\n` +
                    `- ✅ Coverage must be ≥70%\n` +
                    `- ✅ All tests must pass\n` +
                    `- ✅ Check "test-coverage" must complete with success\n\n` +
                    `#### 🔧 How to Fix\n\n` +
                    `1. Add tests to increase coverage to ≥70%\n` +
                    `2. Ensure all existing tests pass\n` +
                    `3. Push changes to trigger new coverage check\n` +
                    `4. Wait for "test-coverage" check to pass\n` +
                    `5. Retry deployment\n\n` +
                    `**Check run:** ${coverageCheck.html_url}`;
                }
              } catch (error) {
                core.info(`Could not check coverage status: ${error.message}`);
              }

              const errorMsg = coverageErrorMsg || `❌ VALIDATION FAILED: PR #${context.issue.number} mergeable_state is "${pr.mergeable_state}". Required: "clean". This PR may have conflicts, required checks failing, or be behind the base branch.`;
              core.setOutput('error_message', errorMsg);
              core.setFailed(errorMsg);
              return { shouldDeploy: false };
            }
            core.info(`✅ Mergeable state check passed: ${pr.mergeable_state}`);

            // SECURITY CHECK 4: Explicit check for merge conflicts
            // Double verification that no conflicts exist
            if (pr.mergeable === false) {
              const errorMsg = `❌ VALIDATION FAILED: PR #${context.issue.number} has merge conflicts. Resolve conflicts before deploying.`;
              core.setOutput('error_message', errorMsg);
              core.setFailed(errorMsg);
              return { shouldDeploy: false };
            }
            core.info(`✅ Merge conflicts check passed: No conflicts detected`);

            // SHA-BASED DEPLOYMENT SECURITY:
            // Extract exact commit SHA from PR HEAD and deploy ONLY that specific commit.
            // This prevents "hitchhiker commits" - a security issue where an attacker pushes
            // additional malicious commits to the PR branch after approval but before deployment.
            //
            // Attack scenario prevented:
            // 1. Attacker creates PR with commit A (looks safe)
            // 2. Owner reviews and approves commit A
            // 3. Attacker pushes commit B (malicious) to same branch
            // 4. Owner triggers deployment thinking they're deploying commit A
            // 5. If deploying by branch name, commit B would be deployed (security breach)
            // 6. With SHA-based deployment, only commit A is deployed (secure)
            //
            // By deploying the exact SHA that exists at comment time, we ensure the deployed
            // code matches what was reviewed, even if the branch is updated afterwards.
            const prSha = pr.head.sha;
            core.info(`📍 Target commit SHA: ${prSha}`);

            // Set outputs for subsequent steps
            core.setOutput('should_deploy', 'true');
            core.setOutput('environment', environment);
            core.setOutput('pr_sha', prSha);
            core.setOutput('pr_number', context.issue.number);
            core.setOutput('pr_branch', pr.head.ref);

            core.info(`✅ All validation checks passed. Proceeding with ${environment} deployment.`);

            return {
              shouldDeploy: true,
              environment: environment,
              prSha: prSha,
              prNumber: context.issue.number
            };

      # STEP 1.5: Create GitHub Deployment via API
      # Creates a deployment record tied to the PR head SHA (not main)
      # This gives correct visibility in GitHub Deployments/Environments UI
      # The job-level environment: setting handles approval gates
      - name: Create GitHub Deployment
        id: create_deployment
        if: steps.validate.outputs.should_deploy == 'true'
        uses: actions/github-script@v7
        with:
          script: |
            const environment = '${{ steps.validate.outputs.environment }}';
            const prSha = '${{ steps.validate.outputs.pr_sha }}';
            const prBranch = '${{ steps.validate.outputs.pr_branch }}';
            const prNumber = '${{ steps.validate.outputs.pr_number }}';

            core.info(`📝 Creating deployment record for ${environment}`);
            core.info(`   Ref (SHA): ${prSha}`);
            core.info(`   Branch: ${prBranch}`);
            core.info(`   PR: #${prNumber}`);

            const { data: deployment } = await github.rest.repos.createDeployment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              ref: prSha,
              task: `${prBranch}:deploy`,  // Shows branch + action in UI (e.g., "feature_1:deploy")
              environment,
              auto_merge: false,
              description: `Deploy PR #${prNumber} (${prBranch}) to ${environment} via comment trigger`,
              payload: {
                pr_number: prNumber,
                pr_branch: prBranch,
                pr_sha: prSha,
                triggered_by: context.payload.comment.user.login,
                comment_id: context.payload.comment.id
              }
            });

            core.setOutput('deployment_id', deployment.id);
            core.info(`Deployment created: ID ${deployment.id}`);

            // Mark as in-progress for better visibility
            await github.rest.repos.createDeploymentStatus({
              owner: context.repo.owner,
              repo: context.repo.repo,
              deployment_id: deployment.id,
              state: 'in_progress',
              environment,
              description: `Deployment started for ${environment}`,
              log_url: `https://github.com/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`
            });

      # STEP 1.6: Verify Coverage Gate Passed
      # COVERAGE GATE: Block deployment if coverage check missing or failed
      # Queries GitHub Checks API for "test-coverage" check run (job name) on the PR SHA
      # Deployment proceeds only if coverage check exists with conclusion=success
      - name: Verify Coverage Gate
        id: verify_coverage
        if: steps.validate.outputs.should_deploy == 'true'
        uses: actions/github-script@v7
        with:
          script: |
            const prSha = '${{ steps.validate.outputs.pr_sha }}';
            const requiredCheckName = 'test-coverage';  // Must match job name in test-coverage.yml

            core.info(`🔍 Checking coverage gate for SHA: ${prSha}`);

            // Query ALL check runs on this SHA (do not use check_name filter to avoid API quirks)
            const { data: allChecks } = await github.rest.checks.listForRef({
              owner: context.repo.owner,
              repo: context.repo.repo,
              ref: prSha
            });

            core.info(`Found ${allChecks.total_count} total check runs for SHA ${prSha.substring(0, 7)}`);

            // Filter to find the coverage check by exact name match
            const coverageChecks = allChecks.check_runs.filter(check => check.name === requiredCheckName);

            core.info(`Found ${coverageChecks.length} check runs with name="${requiredCheckName}"`);

            // Verify coverage check exists
            if (coverageChecks.length === 0) {
              const errorMsg = `❌ COVERAGE GATE FAILED: No "${requiredCheckName}" check found for SHA ${prSha.substring(0, 7)}.\n\nCoverage tests must run before deployment.`;
              core.setOutput('error_message', errorMsg);
              core.setOutput('target_sha', prSha);
              core.setFailed(errorMsg);
              return { passed: false };
            }

            // Sort by completed_at (or started_at if not completed) descending to get most recent
            const sortedChecks = coverageChecks.sort((a, b) => {
              const timeA = new Date(a.completed_at || a.started_at || 0);
              const timeB = new Date(b.completed_at || b.started_at || 0);
              return timeB - timeA; // Newest first
            });

            // Get the most recent check run
            const coverageCheck = sortedChecks[0];
            core.info(`Using check run ID ${coverageCheck.id} (${coverageCheck.completed_at || coverageCheck.started_at})`);
            core.info(`Check status: ${coverageCheck.status}, conclusion: ${coverageCheck.conclusion}`);

            // Verify check is completed
            if (coverageCheck.status !== 'completed') {
              const statusMsg = coverageCheck.status === 'in_progress' ? 'still running' : coverageCheck.status;
              const errorMsg = `❌ COVERAGE GATE FAILED: Coverage check is ${statusMsg}. Wait for coverage tests to complete.\n\nCheck: ${coverageCheck.html_url}`;
              core.setOutput('error_message', errorMsg);
              core.setOutput('target_sha', prSha);
              core.setOutput('check_url', coverageCheck.html_url);
              core.setFailed(errorMsg);
              return { passed: false };
            }

            // Verify check passed
            if (coverageCheck.conclusion !== 'success') {
              const errorMsg = `❌ COVERAGE GATE FAILED: Coverage check ${coverageCheck.conclusion}. Coverage must be ≥70% to deploy.\n\nCheck: ${coverageCheck.html_url}`;
              core.setOutput('error_message', errorMsg);
              core.setOutput('target_sha', prSha);
              core.setOutput('check_url', coverageCheck.html_url);
              core.setFailed(errorMsg);
              return { passed: false };
            }

            // Extract coverage percentage from check run output (if available)
            let coveragePct = 'N/A';
            if (coverageCheck.output && coverageCheck.output.title) {
              // Parse coverage from title like "✅ Coverage 74.0% (≥70%)"
              const match = coverageCheck.output.title.match(/Coverage\s+([\d.]+)%/);
              if (match) {
                coveragePct = match[1];
                core.info(`Extracted coverage: ${coveragePct}%`);
              }
            }

            core.info(`✅ Coverage gate passed`);
            core.setOutput('coverage_check_url', coverageCheck.html_url);
            core.setOutput('coverage_pct', coveragePct);
            core.setOutput('target_sha', prSha);

            return { passed: true, coveragePct };

      # STEP 1.7: Post coverage gate failure message
      # Runs if coverage gate check failed
      # Provides immediate feedback in PR about coverage requirements
      - name: Post Coverage Gate Failure
        if: always() && steps.verify_coverage.outcome == 'failure'
        uses: actions/github-script@v7
        with:
          script: |
            const errorMessage = '${{ steps.verify_coverage.outputs.error_message }}';
            const targetSha = '${{ steps.verify_coverage.outputs.target_sha }}';
            const checkUrl = '${{ steps.verify_coverage.outputs.check_url }}';
            const coveragePct = '${{ steps.verify_coverage.outputs.coverage_pct }}' || 'N/A';

            let coverageInfo = '';
            if (coveragePct !== 'N/A') {
              coverageInfo = `\n**Current coverage:** ${coveragePct}% (threshold: 70%)\n`;
            }

            const comment = `### ❌ Deployment Blocked - Coverage Gate Failed

            **Error Details:**

            ${errorMessage || '❌ Coverage gate check failed - see workflow logs'}
            ${coverageInfo}
            ---

            #### 📊 Coverage Requirements

            Deployments require:
            - ✅ Coverage tests must run on target SHA: \`${targetSha.substring(0, 7)}\`
            - ✅ Coverage must be ≥70%
            - ✅ All tests must pass
            - ✅ Check "test-coverage" must complete with success

            #### 🔧 How to Fix

            1. Push changes to your branch to trigger coverage tests
            2. Verify tests pass with ≥70% coverage
            3. Wait for "test-coverage" check to complete successfully
            4. Retry deployment with \`[staging]\` or \`[prod]\`

            ${checkUrl ? `**Check run:** ${checkUrl}` : ''}

            ---
            📋 **View Deployment Workflow:** [Run #${context.runId}](https://github.com/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId})
            `;

            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body: comment
            });

      # STEP 2: Post validation failure message
      # Runs if validation step completed but deployment was rejected
      # Provides immediate feedback in PR without checking Actions tab
      # Displays the EXACT error message from validation
      - name: Post Validation Failure
        if: always() && steps.validate.outcome == 'failure'
        uses: actions/github-script@v7
        with:
          script: |
            const errorMessage = ${{ toJSON(steps.validate.outputs.error_message) }};

            const comment = `### ❌ Deployment Rejected

            **Error Details:**

            ${errorMessage || '❌ Validation failed - check workflow logs for details'}

            ---
            📋 **View Full Logs:** [Workflow Run #${context.runId}](https://github.com/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId})
            `;

            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body: comment
            });

      # STEP 3: Checkout exact commit SHA from PR HEAD
      # SECURITY: Always checkout by exact SHA, NEVER by branch name
      # NEVER use git pull - always checkout specific commit
      # This ensures we deploy exactly what was validated, not what's currently on the branch
      - name: Checkout PR HEAD commit (exact SHA)
        if: steps.validate.outputs.should_deploy == 'true'
        uses: actions/checkout@v4
        with:
          ref: ${{ steps.validate.outputs.pr_sha }}
          fetch-depth: 1

      # STEP 4: Capture Build Metadata
      # Record deployment metadata for tracking and observability
      - name: Capture Build Metadata
        id: build_metadata
        if: steps.validate.outputs.should_deploy == 'true'
        run: |
          # Capture build timestamp in ISO 8601 format
          BUILD_TIMESTAMP=$(date -u +"%Y-%m-%dT%H:%M:%SZ")
          echo "build_timestamp=${BUILD_TIMESTAMP}" >> $GITHUB_OUTPUT
          echo "📅 Build Timestamp: ${BUILD_TIMESTAMP}"

          # Deployment ID is the GitHub run ID
          echo "deployment_id=${{ github.run_id }}" >> $GITHUB_OUTPUT
          echo "🆔 Deployment ID: ${{ github.run_id }}"

          # Full 40-character commit SHA (already provided by validation step)
          echo "✅ Full Commit SHA: ${{ steps.validate.outputs.pr_sha }}"

      # STEP 5: Check if same version is already deployed
      # Skip deployment if the exact same SHA is already running
      # Prevents unnecessary downtime and resource waste
      - name: Check Already Deployed
        id: check_deployed
        if: steps.validate.outputs.should_deploy == 'true'
        env:
          COMMIT_SHA: ${{ steps.validate.outputs.pr_sha }}
          ENVIRONMENT: ${{ steps.validate.outputs.environment }}
        run: |
          # Check if service directory exists and has the target commit checked out
          if [ -d "${SERVICE_DIR}" ]; then
            cd ${SERVICE_DIR}
            CURRENT_SHA=$(git rev-parse HEAD 2>/dev/null || echo "")

            if [ "$CURRENT_SHA" = "$COMMIT_SHA" ]; then
              # Check if service is actually running
              if pgrep -f "uvicorn app.main" > /dev/null; then
                echo "already_deployed=true" >> $GITHUB_OUTPUT
                echo "✅ Same version already deployed: ${COMMIT_SHA}"
                echo "⏭️  Skipping deployment (no changes)"
                exit 0
              else
                echo "already_deployed=false" >> $GITHUB_OUTPUT
                echo "🔄 Correct SHA checked out but service not running"
              fi
            else
              echo "already_deployed=false" >> $GITHUB_OUTPUT
              echo "🔄 Different version detected"
              echo "   Current: ${CURRENT_SHA:-'(none)'}"
              echo "   Target:  ${COMMIT_SHA}"
            fi
          else
            echo "already_deployed=false" >> $GITHUB_OUTPUT
            echo "📭 Service directory not found"
          fi

      # STEP 6: Post already-deployed notification
      # Informs user that deployment was skipped because same version is running
      - name: Post Already Deployed Status
        if: steps.validate.outputs.should_deploy == 'true' && steps.check_deployed.outputs.already_deployed == 'true'
        uses: actions/github-script@v7
        with:
          script: |
            const environment = '${{ steps.validate.outputs.environment }}';
            const sha = '${{ steps.validate.outputs.pr_sha }}';
            const buildTimestamp = '${{ steps.build_metadata.outputs.build_timestamp }}';
            const deploymentId = '${{ steps.build_metadata.outputs.deployment_id }}';
            const port = environment === 'staging' ? process.env.STAGING_PORT : process.env.PROD_PORT;

            const comment = `### ℹ️ Deployment Skipped (Already Running)

            #### 📋 Current Deployment
            | Field | Value |
            |-------|-------|
            | **Environment** | \`${environment}\` |
            | **Status** | Already Deployed |
            | **Commit SHA** | \`${sha}\` |
            | **Check Timestamp** | \`${buildTimestamp}\` |
            | **Check ID** | \`${deploymentId}\` |
            | **Port** | \`${port}\` |

            ---
            ✅ This exact version is already running in ${environment}.

            #### 🌐 Access Information
            **URL:** http://localhost:${port}/
            **Workflow Run:** [View Logs](https://github.com/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId})

            💡 **Note:** No deployment needed - skipping to avoid unnecessary downtime.
            `;

            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body: comment
            });

      # STEP 7: Deploy to Staging Environment (VM Mode)
      # Checks out target commit via git and restarts service
      # SECURITY: Secrets (API_KEY, DB_HOST) are injected at runtime as environment variables
      - name: Deploy to Staging
        id: deploy_staging
        if: steps.validate.outputs.should_deploy == 'true' && steps.validate.outputs.environment == 'staging' && steps.check_deployed.outputs.already_deployed != 'true'
        env:
          COMMIT_SHA: ${{ steps.validate.outputs.pr_sha }}
          ENVIRONMENT: staging
          PORT: ${{ env.STAGING_PORT }}
          API_KEY: ${{ secrets.API_KEY }}
          DB_HOST: ${{ secrets.DB_HOST }}
        run: |
          set -e

          echo "🚀 Starting staging deployment (VM mode)..."
          echo "📍 Commit SHA: ${COMMIT_SHA}"
          echo "🏷️  Environment: ${ENVIRONMENT}"
          echo "📁 Service Directory: ${SERVICE_DIR}"
          echo "🔌 Port: ${PORT}"

          # Ensure service directory exists
          if [ ! -d "${SERVICE_DIR}" ]; then
            echo "📁 Creating service directory: ${SERVICE_DIR}"
            sudo mkdir -p ${SERVICE_DIR}
            sudo chown -R $(whoami) ${SERVICE_DIR}
            cd ${SERVICE_DIR}
            git clone ${{ github.server_url }}/${{ github.repository }} .
          fi

          # Navigate to service directory
          cd ${SERVICE_DIR}

          # Stop any running service instances
          echo "🛑 Stopping existing service..."
          pkill -f "uvicorn app.main" || true
          sleep 2

          # Fetch and checkout target commit
          echo "📥 Fetching repository updates..."
          git fetch origin
          echo "🔀 Checking out commit: ${COMMIT_SHA}"
          git checkout ${COMMIT_SHA}

          # Install dependencies if requirements.txt exists
          if [ -f requirements.txt ]; then
            echo "📦 Installing dependencies..."
            python3 -m venv venv || true
            source venv/bin/activate
            pip install -q -r requirements.txt
          fi

          # Start service in background
          echo "▶️  Starting service on port ${PORT}..."
          export COMMIT_SHA="${COMMIT_SHA}"
          export ENVIRONMENT="${ENVIRONMENT}"
          export DEPLOYMENT_ID="${{ github.run_id }}"
          export PORT="${PORT}"
          export API_KEY="${API_KEY}"
          export DB_HOST="${DB_HOST}"

          nohup venv/bin/uvicorn app.main:app \
            --host 0.0.0.0 \
            --port ${PORT} \
            > app.log 2>&1 &

          echo "service_pid=$!" >> $GITHUB_OUTPUT

          # Wait for service startup
          echo "⏳ Waiting for service to start..."
          sleep 5

          # Health check: Verify HTTP endpoint is responding
          echo "🏥 Checking service health..."
          MAX_RETRIES=${HEALTH_CHECK_MAX_RETRIES}
          RETRY_COUNT=0
          HEALTH_STATUS="unknown"

          while [ $RETRY_COUNT -lt $MAX_RETRIES ]; do
            if curl -f -s -o /dev/null http://localhost:${PORT}/ 2>/dev/null; then
              HEALTH_STATUS="healthy"
              echo "✅ Service is healthy and responding"
              break
            fi
            RETRY_COUNT=$((RETRY_COUNT + 1))
            echo "⏳ Attempt $RETRY_COUNT/$MAX_RETRIES - waiting for service..."
            sleep ${HEALTH_CHECK_RETRY_SLEEP}
          done

          if [ "$HEALTH_STATUS" != "healthy" ]; then
            echo "❌ Service health check failed"
            echo "📋 Last 50 lines of app.log:"
            tail -50 app.log || true
            echo "readiness_status=failed" >> $GITHUB_OUTPUT
            exit 1
          fi

          echo "readiness_status=${HEALTH_STATUS}" >> $GITHUB_OUTPUT

          echo "✅ Staging deployment successful"
          echo "📁 Directory: ${SERVICE_DIR}"
          echo "🔌 Port: ${PORT}"
          echo "📍 Commit: ${COMMIT_SHA}"
          echo "🏥 Health: ${HEALTH_STATUS}"

      # STEP 8: Verify Staging Image Exists (Production Pre-Check)
      # PRODUCTION IMAGE IMMUTABILITY:
      # Production MUST NOT rebuild the Docker image. Here's why:
      #
      # 1. CONSISTENCY: The staging image was tested. Rebuilding could introduce differences
      #    due to timestamp differences, network conditions, or external dependency updates.
      #
      # 2. SECURITY: Rebuilding creates opportunity for supply chain attacks. If a dependency
      #    is compromised between staging build and production rebuild, production gets the
      #    malicious version while staging had the clean version.
      #
      # 3. COMPLIANCE: Many regulations require that production deployments are exact copies
      #    of tested artifacts, not rebuilt versions.
      #
      # 4. RELIABILITY: "What you test is what you deploy" - the binary deployed to production
      #    must be byte-for-byte identical to what was tested in staging.
      #
      # Therefore, production deployment FAILS if the commit was not tested in staging first.
      # This enforces the staging-first workflow and prevents untested production deployments.
      - name: Verify Staging Deployment Exists
        if: steps.validate.outputs.should_deploy == 'true' && steps.validate.outputs.environment == 'production' && steps.check_deployed.outputs.already_deployed != 'true'
        env:
          COMMIT_SHA: ${{ steps.validate.outputs.pr_sha }}
        run: |
          echo "🔍 Verifying staging deployment for commit: ${COMMIT_SHA}"

          # In VM mode, we verify that this commit exists in the repository
          # Production deployments should only use commits that have been tested
          # The staging-first workflow is enforced by requiring the commit to be merged/available

          cd ${SERVICE_DIR}
          if ! git cat-file -e ${COMMIT_SHA} 2>/dev/null; then
            echo "❌ DEPLOYMENT FAILED"
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            echo "Commit does not exist: ${COMMIT_SHA}"
            echo ""
            echo "Production deployments MUST use tested commits."
            echo "You MUST deploy to staging first:"
            echo "  1. Comment: [staging]"
            echo "  2. Wait for staging deployment to complete"
            echo "  3. Test staging environment"
            echo "  4. Comment: [prod]"
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            exit 1
          fi

          echo "✅ Commit verified: ${COMMIT_SHA}"

      # STEP 9: Deploy to Production Environment (VM Mode)
      # Uses same commit that was tested in staging - NO REBUILD
      # Checks out target commit via git and restarts service on production port
      # SECURITY: Secrets (API_KEY, DB_HOST) are injected at runtime as environment variables
      - name: Deploy to Production
        id: deploy_production
        if: steps.validate.outputs.should_deploy == 'true' && steps.validate.outputs.environment == 'production' && steps.check_deployed.outputs.already_deployed != 'true'
        env:
          COMMIT_SHA: ${{ steps.validate.outputs.pr_sha }}
          ENVIRONMENT: production
          PORT: ${{ env.PROD_PORT }}
          API_KEY: ${{ secrets.API_KEY }}
          DB_HOST: ${{ secrets.DB_HOST }}
        run: |
          set -e

          echo "🚀 Starting production deployment (VM mode)..."
          echo "📍 Commit SHA: ${COMMIT_SHA}"
          echo "🏷️  Environment: ${ENVIRONMENT}"
          echo "📁 Service Directory: ${SERVICE_DIR}"
          echo "🔌 Port: ${PORT}"
          echo "⚠️  Using tested commit (NO REBUILD)"

          # Ensure service directory exists
          if [ ! -d "${SERVICE_DIR}" ]; then
            echo "📁 Creating service directory: ${SERVICE_DIR}"
            sudo mkdir -p ${SERVICE_DIR}
            sudo chown -R $(whoami) ${SERVICE_DIR}
            cd ${SERVICE_DIR}
            git clone ${{ github.server_url }}/${{ github.repository }} .
          fi

          # Navigate to service directory
          cd ${SERVICE_DIR}

          # Stop any running service instances (both staging and production)
          echo "🛑 Stopping existing service instances..."
          pkill -f "uvicorn app.main" || true
          sleep 2

          # Fetch and checkout target commit
          echo "📥 Fetching repository updates..."
          git fetch origin
          echo "🔀 Checking out commit: ${COMMIT_SHA}"
          git checkout ${COMMIT_SHA}

          # Install dependencies if requirements.txt exists
          if [ -f requirements.txt ]; then
            echo "📦 Installing dependencies..."
            python3 -m venv venv || true
            source venv/bin/activate
            pip install -q -r requirements.txt
          fi

          # Start service in background on production port
          echo "▶️  Starting service on port ${PORT}..."
          export COMMIT_SHA="${COMMIT_SHA}"
          export ENVIRONMENT="${ENVIRONMENT}"
          export DEPLOYMENT_ID="${{ github.run_id }}"
          export PORT="${PORT}"
          export API_KEY="${API_KEY}"
          export DB_HOST="${DB_HOST}"

          nohup venv/bin/uvicorn app.main:app \
            --host 0.0.0.0 \
            --port ${PORT} \
            > app.log 2>&1 &

          echo "service_pid=$!" >> $GITHUB_OUTPUT

          # Wait for service startup
          echo "⏳ Waiting for service to start..."
          sleep 5

          # Health check: Verify HTTP endpoint is responding
          echo "🏥 Checking service health..."
          MAX_RETRIES=${HEALTH_CHECK_MAX_RETRIES}
          RETRY_COUNT=0
          HEALTH_STATUS="unknown"

          while [ $RETRY_COUNT -lt $MAX_RETRIES ]; do
            if curl -f -s -o /dev/null http://localhost:${PORT}/ 2>/dev/null; then
              HEALTH_STATUS="healthy"
              echo "✅ Service is healthy and responding"
              break
            fi
            RETRY_COUNT=$((RETRY_COUNT + 1))
            echo "⏳ Attempt $RETRY_COUNT/$MAX_RETRIES - waiting for service..."
            sleep ${HEALTH_CHECK_RETRY_SLEEP}
          done

          if [ "$HEALTH_STATUS" != "healthy" ]; then
            echo "❌ Service health check failed"
            echo "📋 Last 50 lines of app.log:"
            tail -50 app.log || true
            echo "readiness_status=failed" >> $GITHUB_OUTPUT
            exit 1
          fi

          echo "readiness_status=${HEALTH_STATUS}" >> $GITHUB_OUTPUT

          echo "✅ Production deployment successful"
          echo "📁 Directory: ${SERVICE_DIR}"
          echo "🔌 Port: ${PORT}"
          echo "📍 Commit: ${COMMIT_SHA}"
          echo "🏥 Health: ${HEALTH_STATUS}"

      # STEP 10: Post deployment status comment to PR
      # Runs always (success or failure) if validation passed and deployment was attempted
      - name: Post Deployment Status
        if: always() && steps.validate.outputs.should_deploy == 'true' && steps.check_deployed.outputs.already_deployed != 'true'
        uses: actions/github-script@v7
        with:
          script: |
            const environment = '${{ steps.validate.outputs.environment }}';
            const sha = '${{ steps.validate.outputs.pr_sha }}';
            const jobStatus = '${{ job.status }}';
            const prBranch = '${{ steps.validate.outputs.pr_branch }}';
            const buildTimestamp = '${{ steps.build_metadata.outputs.build_timestamp }}';
            const deploymentId = '${{ steps.build_metadata.outputs.deployment_id }}';

            const statusEmoji = jobStatus === 'success' ? '✅' : '❌';
            const statusText = jobStatus === 'success' ? 'SUCCEEDED' : 'FAILED';
            const port = environment === 'staging' ? process.env.STAGING_PORT : process.env.PROD_PORT;

            // Get environment-specific outputs
            const deployStep = environment === 'staging' ? 'deploy_staging' : 'deploy_production';
            const readinessStatus = environment === 'staging'
              ? '${{ steps.deploy_staging.outputs.readiness_status }}'
              : '${{ steps.deploy_production.outputs.readiness_status }}';

            // Map readiness status to display text
            const readinessMap = {
              'healthy': '✅ Healthy (verified)',
              'running_unverified': '⚠️ Running (unverified)',
              'failed': '❌ Failed',
              '': jobStatus === 'success' ? '✅ Running' : '❌ Failed'
            };
            const readinessDisplay = readinessMap[readinessStatus] || readinessStatus;

            let comment = `### ${statusEmoji} Deployment ${statusText}

            #### 📋 Deployment Details
            | Field | Value |
            |-------|-------|
            | **Environment** | \`${environment}\` |
            | **Status** | ${statusText} |
            | **Commit SHA** | \`${sha}\` |
            | **Build Timestamp** | \`${buildTimestamp}\` |
            | **Deployment ID** | \`${deploymentId}\` |
            | **Port** | \`${port}\` |
            | **Service Status** | ${readinessDisplay} |
            `;

            if (jobStatus === 'success') {
              comment += `\n---\n#### 🌐 Access Information\n`;
              comment += `**URL:** http://localhost:${port}/\n`;
              comment += `**Workflow Run:** [View Logs](https://github.com/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId})\n`;

              if (environment === 'staging') {
                comment += `\n💡 **Next step:** Test staging, then deploy to production with: \`[prod]\``;
              }
            } else {
              comment += `\n---\n#### ⚠️ Action Required\n`;
              comment += `Check workflow logs for detailed error information.\n\n`;
              comment += `**Workflow Run:** [View Logs](https://github.com/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId})\n`;
            }

            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body: comment
            });

      # STEP 11: Update GitHub Deployment Status
      # Updates the deployment record created in step 1.5 with final status
      # Runs always (success/failure/cancelled) if deployment was created
      - name: Update Deployment Status
        if: always() && steps.create_deployment.outputs.deployment_id
        uses: actions/github-script@v7
        with:
          script: |
            const deploymentId = Number('${{ steps.create_deployment.outputs.deployment_id }}');
            const environment = '${{ steps.validate.outputs.environment }}';
            const jobStatus = '${{ job.status }}';
            const alreadyDeployed = '${{ steps.check_deployed.outputs.already_deployed }}' === 'true';
            const prSha = '${{ steps.validate.outputs.pr_sha }}';

            // Sanitizer to remove 4-byte Unicode characters (emoji) that GitHub API rejects
            const sanitize = (s) => Array.from(s)
              .filter(ch => ch.codePointAt(0) <= 0xFFFF)
              .join('');

            let state;
            let description;

            if (alreadyDeployed) {
              state = 'success';
              description = `Skipped - SHA ${prSha.substring(0, 7)} already running in ${environment}`;
            } else if (jobStatus === 'success') {
              state = 'success';
              description = `Deployed SHA ${prSha.substring(0, 7)} to ${environment}`;
            } else if (jobStatus === 'failure') {
              state = 'failure';
              description = `Deployment to ${environment} failed`;
            } else {
              state = 'error';
              description = `Deployment to ${environment} ended with status: ${jobStatus}`;
            }

            // Sanitize description to prevent 422 errors from emoji/4-byte chars
            description = sanitize(description);

            const port = environment === 'staging' ? process.env.STAGING_PORT : process.env.PROD_PORT;
            const environmentUrl = `http://localhost:${port}/`;
            const logUrl = `https://github.com/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`;

            await github.rest.repos.createDeploymentStatus({
              owner: context.repo.owner,
              repo: context.repo.repo,
              deployment_id: deploymentId,
              state,
              environment,
              description,
              environment_url: environmentUrl,
              log_url: logUrl,
              auto_inactive: true
            });

            core.info(`Deployment status updated: ${state}`);
